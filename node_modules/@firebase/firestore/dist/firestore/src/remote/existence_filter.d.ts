
import { BloomFilter as ProtoBloomFilter } from '../protos/firestore_proto_api';
export declare class ExistenceFilter {
    count: number;
    unchangedNames?: ProtoBloomFilter | undefined;
    constructor(count: number, unchangedNames?: ProtoBloomFilter | undefined);
}
